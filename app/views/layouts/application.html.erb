<!DOCTYPE html>
<html>
<head>
  <title>Open Source Intergroup Meetings</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <!-- need /app/assets/js/bootstrap.min.js to work on android devices -->
  <%= csrf_meta_tags %>
   <!--[if lt IE 9]>
     <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"     type="text/javascript"></script>
   <![endif]-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<%= render 'layouts/header' %>

<div class="container-fluid">
	<div id="storylinks-container">
     <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

       <div class="outer">
			<%= yield %>
      <%= debug(params) if Rails.env.development? %>
		</div>	
	</div>
      <div class="copyright">&#169; 2013</div>
      <div id="colophon"><%= link_to 'colophon', colophon_path %></div>
</div>

</body>
</html>
